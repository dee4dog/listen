# GUI
PySide6>=6.6

# Speech-to-text (downloads Whisper models on first run, runs on CPU)
faster-whisper>=1.0

# Speaker separation (voice embeddings + clustering)
torch>=2.1
torchaudio>=2.1
speechbrain>=1.0
scikit-learn>=1.3
numpy>=1.24

# Audio capture (microphone + Windows WASAPI loopback for Teams/Meet audio)
soundcard>=0.4.3

# Exports
python-docx>=1.1
openpyxl>=3.1
reportlab>=4.0

# Central-server sync (the server itself, server/central_server.py, needs no packages)
requests>=2.28
